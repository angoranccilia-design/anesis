{
  "name": "@anesis/campaign",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ANESIS campaign CLI (Aug 4 diagnostics): Apify preflight, prospect import, assessment batch, manual-review triage.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node --env-file=.env --import tsx src/run.ts"
  },
  "dependencies": {
    "@anesis/agent-runtime": "workspace:*",
    "@anesis/core": "workspace:*",
    "@anesis/db": "workspace:*",
    "@anesis/events": "workspace:*",
    "@anesis/sources": "workspace:*",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
