{
  "name": "@anesis/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ANESIS auth — magic-link sign-in for operators. Pure token logic + Resend adapter behind an env var (no-op in dev).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anesis/core": "workspace:*",
    "@anesis/db": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
