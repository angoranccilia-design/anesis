{
  "name": "@anesis/sources",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ANESIS signal sources (IO adapters) behind the assessment RawObservations interface — PageSpeed, reviews (Apify), HTML.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anesis/assessment": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
