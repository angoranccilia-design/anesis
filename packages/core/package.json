{
  "name": "@anesis/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ANESIS domain model — pure types, state machines and invariants. Zero runtime dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./unsafe": "./src/unsafe.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
